# 医院预约系统 · 搭建步骤记录

> 目标：打造一个与市面上预约小程序无差别的**医院全栈应用**（前端 + 后端 + 数据库）。
> 技术栈：Vue 3 + Vite + **Element Plus**（vue3 版 element-ui）+ Vue Router + Pinia + Node.js(Express) + MySQL。
> 完整运行说明见 `README.md`。

---

### ✅ 第 1 步：架构设计（2026-08-10）
- 全栈三层架构：`vue-app/`(前端) + `backend/`(API服务) + `database/`(MySQL脚本)。
- **Element Plus** 替代 element-ui（后者不支持 Vue 3）。
- 数据库：MySQL 8，Navicat Premium 17 可直接连接。
- 认证：JWT Token；支付：模拟支付（微信/支付宝/医保，不真实扣款）。
- 前端采用移动端小程序风格：底部 TabBar（首页/科室/我的预约/我的）。
- 配置 Vite 代理：`/api` → `http://localhost:3000`。

### ✅ 第 2 步：安装依赖
- 后端：express / cors / mysql2 / jsonwebtoken / bcryptjs / dotenv / nodemon
- 前端：element-plus / @element-plus/icons-vue / axios

### ✅ 第 3 步：数据库设计
- 建库 hospital_db，10 张表：用户/科室/医生/排班/挂号/药品/处方/处方明细/支付/医院简介
- 种子数据（全部不重复）：10 科室、20 医生、7 天排班、24 药品、医院信息
- 文件：database/schema.sql、seed_data.sql、README.md（Navicat 可直接导入）
- 附加 refresh_schedules.sql：排班过期时一键刷新为当前日期

### ✅ 第 4 步：后端 API
- Express + MySQL 连接池 + JWT 登录认证，模拟支付（微信/支付宝/医保）
- 接口：注册登录/科室/医生/排班/挂号/取消/支付/药品/处方取药
- 支付成功自动生成电子处方，演示取药全流程
- 挂号/取消/支付用事务 + FOR UPDATE 行锁防超卖

### ✅ 第 5 步：前端重写（Element Plus）
- Vue3 + Element Plus + Pinia + 路由守卫，移动端小程序风格（底部 TabBar）
- 页面：登录注册/首页/科室/医生列表/医生详情/挂号支付/我的预约/药品库/处方取药/个人中心
- 复用组件：DoctorCard/DepartmentCard/SectionTitle/StatusTag/EmptyState/PubPaymentDialog（多页面复用）
- 路由：嵌套布局 + 命名路由 + 动态参数 + 编程式跳转 + 登录守卫

### ✅ 第 6 步：构建验证
- 前端 npm run build 通过，产物 dist/ 生成（1695 模块，0 错误）
- 后端 node --check 语法检查 11/11 全部通过

### ✅ 第 7 步：集成审查与修复
- 发现前端调用的 `/api/hospital/profile` 后端缺失 → 新增 hospital 路由并挂载
- 验证前后端 API 契约一致（科室/医生/排班/挂号/处方字段全部对齐）
- 生成 refresh_schedules.sql 解决排班日期过期问题

### ✅ 第 8 步：Docker 完整部署
- 三服务 docker-compose：MySQL(自动导入SQL) + 后端 + 前端(Nginx 反向代理 /api)
- 前端 nginx.conf 增加 /api → backend:3000 代理
- 一键启动：`docker compose up -d --build`，访问 http://localhost:8080
