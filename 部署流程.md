# 🚀 医院预约系统 · 上线部署流程（8 步）

> 场景：本地开发已完成，现有一台**空虚拟机**（`192.168.229.191`）作为服务器，需要将系统从「本地可跑」推进到「服务器上线」。
> 目标：任何一步执行后，都能明确「我做到哪了」「下一步做什么」。

---

## 一、整体流程图

```
本地开发(已完成) → Git 托管 → 虚拟机装系统+Docker → 传项目
     → docker compose 一键部署 → Navicat 连数据库 → 放行端口 → 日常维护上线
```

---

## 二、流程要求（前置条件）

| 序号 | 要求 | 说明 |
|------|------|------|
| 1 | 一台可用的虚拟机 | 地址 `192.168.229.191`，已安装 **Linux 系统**（推荐 Ubuntu 22.04） |
| 2 | 可远程连接虚拟机 | Windows 能 `ping 192.168.229.191`，可用 SSH / WinSCP 登录 |
| 3 | Docker 环境 | 在虚拟机上安装 Docker（唯一必装软件） |
| 4 | MySQL 数据库 | **不用单独装**，由 Docker Compose 自动启动并导入数据 |
| 5 | Navicat Premium 17 | 管理/查看数据库，连接虚拟机上的 MySQL |
| 6 | 项目代码 | 本地项目上传到虚拟机，或 Git 拉取 |
| 7 | 端口开放 | 3306 / 3000 / 8080 三个端口需放行 |

> ⚠️ 明确：本流程是**局域网部署**，仅自己可访问。若要公网访问需云服务器或内网穿透（见第八节）。

---

## 三、8 步详细操作

### 第 1 步：开发期（已完成 ✅）

在 Windows 本地编写代码、`npm run dev` 调试通过。

- ✅ 前端 `vue-app/`、后端 `backend/`、数据库 `database/` 已开发完成
- ✅ 本地构建验证通过（前端 build 0 错误，后端语法检查通过）

**本步已完成，无需操作。**

---

### 第 2 步：代码管理（Git 托管）

目的：让虚拟机用 `git clone` 拉取代码，以后改代码便于同步。

```bash
# 1. 初始化并提交（在项目目录执行一次）
git init
git add .
git commit -m "医院预约系统 v1.0"

# 2. 推到远程仓库（Gitee / GitHub 私有仓库）
git remote add origin 你的仓库地址
git push -u origin main
```

> 不想用 Git？可跳过本步，用 WinSCP 直接把项目文件夹拖到虚拟机（见第 4 步）。

---

### 第 3 步：虚拟机装系统 + Docker

**3.1 确认虚拟机已装 Linux**
- 若未装系统：在 VMware 里安装 **Ubuntu 22.04**（网络模式选**桥接 Bridged**，否则宿主机可能连不上）。
- 装好后，Windows 终端连接：

```bash
ssh root@192.168.229.191     # 或 ssh 用户名@192.168.229.191
```

**3.2 安装 Docker（Ubuntu 一键脚本）**

```bash
curl -fsSL https://get.docker.com | sh
sudo systemctl enable --now docker
sudo docker version            # 验证安装成功
```

**✅ 检查点**：执行 `sudo docker ps` 不报错，说明 Docker 就绪。

---

### 第 4 步：把项目传到虚拟机

在 Windows 上，把整个项目文件夹（含 `docker-compose.yml`、`backend/`、`vue-app/`、`database/`）传到虚拟机。

**方式 A：Git（推荐）**
```bash
# 在虚拟机里
git clone 你的仓库地址
cd 项目目录
```

**方式 B：WinSCP / FinalShell 手动上传**
1. Windows 打开 WinSCP，协议 SFTP，填 `192.168.229.191` + 账号密码
2. 把项目文件夹整个拖到虚拟机（如 `/root/app`）

**✅ 检查点**：虚拟机里能看到 `docker-compose.yml` 文件即成功。

---

### 第 5 步：一键部署（docker compose）

```bash
cd 项目目录
docker compose up -d --build
```

命令自动完成：
1. 启动 **MySQL**（自动导入 10 张表 + 种子数据 + 刷新当前排班）
2. 启动 **后端**（等待 MySQL 就绪后连接）
3. 构建并启动 **前端**（Nginx 反向代理 `/api` → 后端）

查看运行状态：

```bash
docker compose ps                    # 三个服务都应为 Up
docker compose logs -f backend       # 查看后端日志（应看到演示账号创建成功）
```

**✅ 检查点**：浏览器访问 `http://192.168.229.191:8080` 能打开登录页。

| 服务 | 地址 |
|------|------|
| 前端 | http://192.168.229.191:8080 |
| 后端 API | http://192.168.229.191:3000/api |
| MySQL | 192.168.229.191:3306 |

---

### 第 6 步：Navicat 连接数据库

1. 打开 Navicat Premium 17 → 新建连接 → MySQL
2. 填写连接信息：

| 配置项 | 值 |
|--------|-----|
| 连接名 | 任意，如 `医院虚拟机` |
| 主机 | `192.168.229.191` |
| 端口 | `3306` |
| 用户名 | `root` |
| 密码 | `root123456` |

3. 点击「测试连接」→ 成功即完成。
4. 双击 `hospital_db` 数据库，即可查看/管理 10 张表的数据。

> 💡 排班日期过期时：在 Navicat 查询窗口运行 `database/refresh_schedules.sql` 刷新为当前日期。

---

### 第 7 步：端口放行（新手最易踩坑）

Windows 访问不到虚拟机服务，90% 是端口没放行。三处逐一检查：

**① 虚拟机防火墙放行端口**
```bash
sudo ufw allow 3306/tcp
sudo ufw allow 3000/tcp
sudo ufw allow 8080/tcp
sudo ufw enable
```

**② 虚拟机网络模式**
- VMware 设置里改为「**桥接模式 (Bridged)**」。
- 确保 Windows 能 `ping 192.168.229.191`。

**③ MySQL 远程连接（Docker 方式已默认允许）**
- 本流程用 Docker 启动的 MySQL 默认允许 `root` 远程登录，无需额外配置。
- 若为直装 MySQL，需创建远程用户：`CREATE USER 'root'@'%' IDENTIFIED BY 'root123456'; GRANT ALL ON *.* TO 'root'@'%';`

**✅ 检查点**：Navicat 测试连接成功 + 浏览器打开页面，即全部放行。

---

### 第 8 步：日常维护

```bash
docker compose ps                    # 查看三个服务状态
docker compose logs -f backend       # 实时查看后端日志
docker compose up -d --build         # 更新代码后重新部署（增量，很快）
docker compose down                  # 停止所有服务
docker compose down -v               # 停止并删除数据卷（⚠️ 会清空数据库数据）
```

**排班刷新**：排班种子只有 7 天，过期后执行：
```bash
docker compose exec mysql mysql -uroot -proot123456 hospital_db < database/refresh_schedules.sql
```

---

## 四、故障排查速查表

| 现象 | 原因 | 解决 |
|------|------|------|
| `ping 192.168.229.191` 不通 | 网络模式不对 / 系统未装 | VMware 改桥接模式，确认已装 Linux |
| Navicat 连不上 3306 | 端口未放行 / MySQL 未启动 | 执行第 7 步 `ufw allow`；确认 `docker compose ps` 中 mysql 为 Up |
| 浏览器打不开 8080 | 端口未放行 / 前端构建失败 | 放行 8080；`docker compose logs frontend` 查看 |
| 登录后页面无数据 | 后端连不上 MySQL | `docker compose logs backend` 看报错；确认 MySQL 健康 |
| 挂号显示「没有号源」 | 排班日期已过期 | 运行 `refresh_schedules.sql` 刷新 |
| 改代码不生效 | 需要重新构建镜像 | 重新执行 `docker compose up -d --build` |

---

## 五、检查清单（上线确认）

- [ ] 虚拟机已装 Linux，SSH 能登录
- [ ] Docker 已安装并启动
- [ ] 项目已上传到虚拟机
- [ ] `docker compose up -d --build` 三个服务全部 Up
- [ ] 浏览器能打开 `http://192.168.229.191:8080` 并登录体验
- [ ] Navicat 能连接 `192.168.229.191:3306`（root/root123456）
- [ ] 3306 / 3000 / 8080 端口已放行

---

## 六、重要提醒

1. **虚拟机 = 局域网服务器**：`192.168.229.191` 只有你自己的设备能访问。要让**别人**通过互联网访问：
   - 方案一：购买**云服务器**（阿里云/腾讯云轻量服务器，约几十元/年），把本流程重复一遍；
   - 方案二：用**内网穿透**（如 cpolar / frp）将 8080 端口映射到公网。
   - 学习阶段用虚拟机完全足够，功能体验无差别。
2. **数据库密码**：本流程统一使用 `root123456`，部署后可到 `docker-compose.yml` 和 `backend/.env` 修改。
3. **数据备份**：升级前先 `docker compose down` 并用 Navicat 导出 `hospital_db` 备份，防操作失误。
