# 仁爱医院预约系统 · 改进流程书

> 版本：v1.0 · 2026-08-13
> 用途：记录系统当前状态、待修复问题与后续升级路线。每完成一项，在文末"完成度勾选清单"打勾，并把日期写上去。
> 原则：**小步提交、可运行可验收**。每做完一个阶段就 `git commit` + 部署验证，不要憋一个大版本。

---

## 一、系统现状

### 1.1 已实现功能（均可正常使用）

| 模块 | 功能 |
| --- | --- |
| 账号 | 注册 / 登录（JWT 鉴权）、个人中心、演示账号 |
| 科室 | 科室导航、科室简介、科室医生列表 |
| 医生 | 医生列表、医生详情（擅长/简介自动补齐）、未来 7 天排班、余号/约满展示 |
| 挂号 | 选排班 → 确认挂号 → 生成待支付订单、排队号、扣减号源 |
| 支付 | 模拟支付（微信/支付宝/医保）、生成唯一支付凭证号（13 位数字 + 2 位字母）、记录支付时间 |
| 就诊闭环 | 支付后生成就诊码（= 支付凭证号）→ 到院签到「待就诊」→ 接诊完成「已完成」 |
| 预约 | 我的预约列表（预约时间/支付时间/就诊码）、取消预约（恢复号源） |
| 处方 | 接诊完成后自动生成电子处方、药品明细、模拟取药 |
| 药品 | 药品查询 |
| 管理后台 | 管理员身份（admin/admin123）、科室/医生增删改查、全量预约查询与状态管理、处方查询、统计概览、手动生成排班 |
| 导航 | 浮动返回/首页按钮（首页自动隐藏）、子页返回栏 + 首页按钮 |
| 部署 | Docker 一键部署（`docker-compose.server.yml`）、SPA history 路由、nginx 反代 |

### 1.2 技术栈

- 前端：Vue3 + Vite + Element Plus（移动优先，内容区 max-width 480px）
- 后端：Node.js + Express + mysql2/promise，JWT 鉴权
- 数据库：MySQL（10 张表：users / departments / doctors / schedules / appointments / medicines / prescriptions / prescription_items / payments / hospital_profile）
- 部署：CentOS 7 虚拟机 `192.168.229.191`，原生 MySQL + Docker（前端 nginx / 后端 node）

### 1.3 接口约定（已统一）

- 统一前缀 `/api`，成功 `{ code:0, message:'ok', data }`，失败 `{ code:1, message }`
- 数据字段统一 **camelCase**（前端 request.js 兜底把下划线转驼峰）
- 详情类接口包一层对象：`{ doctor }` / `{ department }` / `{ prescription }` / `{ appointment }` / `{ payment }`（前端 API 层已解包）

---

## 二、当前已知问题（建议立即处理）

| 优先级 | 问题 | 说明 | 建议方案 |
| --- | --- | --- | --- |
| 🔴 严重 | **排班数据过期** | seed 只排到 2026-08-17，之后所有医生无号可挂，挂号功能瘫痪 | ✅ 已处理（阶段一 2026-08-13）：后端启动自动补"未来 7 天"排班，幂等 |
| 🟡 中等 | 支付为纯模拟 | 点一下就成功，没有支付中间状态 | 阶段一加"支付中"转场，或阶段三接支付沙箱 |
| 🟢 轻微 | 头像为文字首字占位 | 视觉单调 | 阶段三换真实头像 |
| 🟢 轻微 | 无测试、无自动部署 | 全靠手动 `git pull` | 阶段四工程化 |

---

## 三、改进路线总览

```
阶段一  就诊闭环 + 排班自动生成   ← 推荐先做（量适中，解决硬伤，闭环演示）
阶段二  管理后台                 ← 工程量最大，最像真产品
阶段三  视觉与真实感             ← 工作量最小，最快出效果，可随时穿插
阶段四  工程化（测试 + CI 部署） ← 规范化，长远受益
```

---

## 四、阶段一：就诊闭环 + 排班自动生成

**目标**：解决排班过期硬伤；把流程从"挂号→支付→处方"补全为"挂号→支付→签到→就诊→处方"，并给每单一个就诊码。

### 4.1 排班自动生成（必须先做，否则 4 天后系统瘫痪）

- **后端**：新增 `backend/src/utils/scheduleGenerator.js`，逻辑复用 `database/refresh_schedules.sql`（所有医生 × 未来 7 天，上午必出、下午按 `(doctor_id + 天数) % 3` 轮换，`remaining_slots = 20 - 公式`）。
- **触发方式**（二选一，建议都做）：
  1. 后端启动时（`server.js` 内）：检测未来 7 天缺哪天排班，缺则补生成（**幂等**，重复启动不重复插）；
  2. 提供管理接口 `POST /api/admin/schedules/generate`（配合阶段二后台按钮，随时手动刷新）。
- **数据库**：`schedules` 表有唯一键 `(doctor_id, work_date, period)`，用"先查后插 / `INSERT ... ON DUPLICATE KEY UPDATE`"保证幂等。
- **验收**：把服务器系统时间改到 08-18 再访问医生详情，仍能看到未来 7 天排班；重启后端不会生成重复排班。

### 4.2 就诊闭环（签到 → 就诊）

现状：支付成功后**立刻**生成处方、状态直接"已支付"。
改动目标：支付 → 已支付（生成就诊码）→ 到院签到 → 待就诊 → 接诊完成 → 已完成（此刻才生成处方）。

- **数据库**：
  - `appointments.status` 的 ENUM 增加 `'待就诊'`（`ALTER TABLE appointments MODIFY status ENUM('待支付','已支付','待就诊','已完成','已取消')`）；
  - `payments` 表已存支付信息，就诊码可复用 `payment_no`（13 位数字 + 2 位字母），无需新增列。
- **后端** `backend/src/routes/appointments.js`：
  - `POST /:id/checkin`：`已支付 → 待就诊`，校验订单归属；
  - `POST /:id/finish`：`待就诊 → 已完成`，**把 `createPrescription` 调用从支付接口迁移到这里**（支付时不再生成处方）；
  - `GET /my` 返回状态对应就诊码（已支付以上状态都可看到 `paymentNo`）。
- **前端**：
  - 我的预约卡（`Appointments.vue`）：已支付/待就诊显示就诊码 + 「到院签到」按钮（待就诊后变「已完成」不可再点或隐藏）；
  - 签到成功提示，状态刷新。
- **行为变更提醒**：处方从"支付即有"变为"接诊完成才有"——演示流程要相应调整为：挂号 → 支付 → 签到 → 就诊 → 看处方。
- **验收**：挂号支付后我的预约出现就诊码；点签到变"待就诊"；点接诊完成变"已完成"且此时"我的处方"才出现新处方；期间不能跳过签到直接完成。

---

## 五、阶段二：管理后台

**目标**：给系统加"管理员"身份与后台管理界面，管理数据、查看全量订单。

### 5.1 管理员身份

- **数据库**：`users` 表加 `role ENUM('user','admin') NOT NULL DEFAULT 'user'`，seed 一个管理员（如 `admin / admin123`）。
- **后端** `backend/src/middleware/auth.js`：新增 `requireAdmin` 中间件（校验 `req.user.role === 'admin'`）；登录接口返回 `role`，`/api/auth/me` 返回 `role`。

### 5.2 管理接口（`/api/admin/*`）

| 接口 | 功能 |
| --- | --- |
| `GET/POST/PUT/DELETE /api/admin/departments` | 科室增删改查 |
| `GET/POST/PUT/DELETE /api/admin/doctors` | 医生增删改查 |
| `POST /api/admin/schedules/generate` | 手动生成排班（接阶段一 4.1） |
| `GET /api/admin/appointments` | 全量预约查询（可按状态/日期/医生过滤） |
| `GET /api/admin/prescriptions` | 全量处方查询 |
| `GET /api/admin/stats` | 统计：今日挂号量、待就诊数、支付金额合计、医生工作量 |

### 5.3 管理前端

- 新路由 `/admin`（独立布局，桌面端表格 + Element Plus 表单，不做移动优先）；
- 登录后根据 `role` 决定入口（个人中心显示"进入管理后台"）；
- 每张表：列表 + 搜索 + 新增/编辑弹窗 + 删除确认。

- **验收**：管理员登录可见后台入口；能新增医生并立刻在患者端首页出现；能查看任意用户预约并改状态；普通用户无后台入口、直接访问 `/admin` 被拦。

---

## 六、阶段三：视觉与真实感

**目标**：快速提升观感，工作量最小，可随时穿插在阶段一/二之间。

| 子项 | 做法 | 工作量 |
| --- | --- | --- |
| 真实头像 | 前端按名字生成头像 URL：`https://api.dicebear.com/9.x/initials/svg?seed=${name}`，后端 `avatar` 为空时用该 URL | 小 |
| 就诊码二维码 | 前端引入 `qrcode` 库，把支付凭证号/订单号渲染成二维码，展示在"我的预约" | 小 |
| PWA 可安装 | `vite-plugin-pwa`，手机浏览器"添加到桌面"，像 App 一样打开 | 中 |
| 桌面端适配 | 取消 `body max-width:480px` 的硬限制，宽屏下卡片网格/侧边栏 | 中 |
| 加载体验 | 骨架屏（医生卡/预约卡 loading 态） | 小 |

- **验收**：医生卡显示真人头像；我的预约长按可保存就诊码图片；手机浏览器可添加到桌面并离线打开壳页面。

---

## 七、阶段四：工程化（测试 + CI 部署）

**目标**：代码可信、部署自动。

### 7.1 单元测试

- 后端：`jest` + `supertest`，覆盖核心逻辑——排班生成幂等、挂号扣号源、并发防超卖、取消恢复号源、处方生成（项目已预留 `_createPrescription` 测试钩子）。
- 前端：`vitest`，覆盖 `request.js` 的 `camelizeKeys` / 401 分流逻辑。

### 7.2 自动部署（GitHub Actions）

- 新建 `.github/workflows/deploy.yml`：
  1. `push master` 触发；
  2. 构建前端 + 后端测试；
  3. SSH 到 `192.168.229.191` 执行 `git pull && docker compose -f docker-compose.server.yml up -d --build`；
- 需在 VM 配置 SSH 部署密钥（`~/.ssh/authorized_keys`）与仓库 Secrets（`VM_HOST / VM_USER / VM_SSH_KEY`）。
- ⚠️ 注意：部署脚本不得执行"清库/重新导入 seed"操作，避免冲掉已注册账号。

### 7.3 接口文档

- 手写 `docs/api.md`（按模块列出接口、参数、返回示例），或引入 OpenAPI 自动生成。

- **验收**：`git push` 后 1 分钟内服务器自动更新；`npm test` 全绿；接口文档能对着调通每个接口。

---

## 八、执行建议

1. **顺序**：阶段一(4.1 排班) → 阶段一(4.2 闭环) → 阶段二 → 阶段三/四穿插。
2. **每阶段完成标准**：本地 `npm run build` 通过 → 后端 `node --check` 通过 → 提交推送 → VM 重新构建 → 浏览器 Ctrl+F5 按"验收标准"走一遍。
3. **每阶段都单独提交**，commit message 写清楚改了什么，方便出问题回退。
4. 改到 **数据表结构**（如加 `role`、改 ENUM）时，记得同步更新 `database/schema.sql` 与部署环境实际执行的建表语句，否则部署环境与本地不一致。
5. 所有改动**不破坏现有演示账号**（不要跑清库脚本）。

---

## 九、完成度勾选清单

### 阶段一：就诊闭环 + 排班自动生成
- [x] 排班自动生成（后端启动补缺，幂等）— 2026-08-13（管理接口触发留待阶段二）
- [x] `appointments.status` 增加 `待就诊` — 2026-08-13（启动时自动 ALTER + schema.sql 已同步）
- [x] 签到接口 `POST /appointments/:id/checkin` — 2026-08-13
- [x] 接诊完成接口 `POST /appointments/:id/finish`（处方生成迁移至此）— 2026-08-13
- [x] 我的预约显示就诊码 + 签到/接诊按钮 — 2026-08-13
- [ ] 阶段一全流程验收通过（挂号→支付→签到→就诊→处方）— 待 VM 部署后走一遍

### 阶段二：管理后台
- [x] `users.role` 字段 + 管理员 seed + `requireAdmin` 中间件 — 2026-08-13
- [x] `/api/admin/*` 全部接口 — 2026-08-13
- [x] 管理前端页面（科室/医生/排班/预约/处方/统计）— 2026-08-13
- [ ] 阶段二验收通过（admin/admin123 登录→后台入口→新增医生→普通用户被拦）— 待 VM 部署验证

### 阶段三：视觉与真实感
- [ ] 真实头像
- [ ] 就诊码二维码
- [ ] PWA 可安装
- [ ] 桌面端适配

### 阶段四：工程化
- [ ] 后端单元测试（挂号/排班/处方核心逻辑）
- [ ] 前端单元测试（request 拦截器）
- [ ] GitHub Actions 自动部署
- [ ] 接口文档 `docs/api.md`
